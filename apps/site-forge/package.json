{
  "name": "@webcules/site-forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "$10k-website generator: UI research → local ComfyUI asset generation → Astro site → Cloudflare. Zero runtime dependencies (Node 22+ type stripping).",
  "scripts": {
    "forge": "node --disable-warning=ExperimentalWarning src/cli.ts",
    "doctor": "node src/cli.ts doctor",
    "build": "tsc --noEmit",
    "dev": "node src/cli.ts help"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@webcules/typescript-config": "workspace:*",
    "typescript": "5.7.3"
  }
}
