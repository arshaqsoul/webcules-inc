---
import Layout from "../../layouts/Layout.astro";
import Workspace from "../../components/Workspace.tsx";
const { slug } = Astro.params;
---
<Layout title={`social-forge — ${slug}`}>
  <Workspace slug={slug!} client:load />
</Layout>
